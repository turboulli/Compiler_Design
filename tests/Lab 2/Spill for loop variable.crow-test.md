# Executing your compiler

## ShouldSucceed

## ProgramArgumentFile

```
int main() {
  int x = 0;
  int y = (x + 1) / 1;

  for (int c01 = 0x01; true; ) { for (int c02 = 0x02; true; ) {
  for (int c03 = 0x03; true; ) { for (int c04 = 0x04; true; ) {
  for (int c05 = 0x05; true; ) { for (int c06 = 0x06; true; ) {
  for (int c07 = 0x07; true; ) { for (int c08 = 0x08; true; ) {
  for (int c09 = 0x09; true; ) { for (int c0A = 0x0A; true; ) {
  for (int c0B = 0x0B; true; ) { for (int c0C = 0x0C; true; ) {
  for (int c0D = 0x0D; true; ) { for (int c0E = 0x0E; true; ) {
  for (int c0F = 0x0F; true; ) { for (int c10 = 0x10; true; ) {
  for (int c11 = 0x11; true; ) { for (int c12 = 0x12; true; ) {
  for (int c13 = 0x13; true; ) { for (int c14 = 0x14; true; ) {
  for (int c15 = 0x15; true; ) { for (int c16 = 0x16; true; ) {
  for (int c17 = 0x17; true; ) { for (int c18 = 0x18; true; ) {
  for (int c19 = 0x19; true; ) { for (int c1A = 0x1A; true; ) {
  for (int c1B = 0x1B; true; ) { for (int c1C = 0x1C; true; ) {
  for (int c1D = 0x1D; true; ) { for (int c1E = 0x1E; true; ) {
  for (int c1F = 0x1F; true; ) { for (int c20 = 0x20; true; ) {

    x |= (c20 / y) | (c01 / y);
    x |= (c1F / y) | (c02 / y);
    x |= (c1E / y) | (c03 / y);
    x |= (c1D / y) | (c04 / y);
    x |= (c1C / y) | (c05 / y);
    x |= (c1B / y) | (c06 / y);
    x |= (c1A / y) | (c07 / y);
    x |= (c19 / y) | (c08 / y);
    x |= (c18 / y) | (c09 / y);
    x |= (c17 / y) | (c0A / y);
    x |= (c16 / y) | (c0B / y);
    x |= (c15 / y) | (c0C / y);
    x |= (c14 / y) | (c0D / y);
    x |= (c13 / y) | (c0E / y);
    x |= (c12 / y) | (c0F / y);
    x |= (c11 / y) | (c10 / y);
    x |= (c10 / y) | (c11 / y);
    x |= (c0F / y) | (c12 / y);
    x |= (c0E / y) | (c13 / y);
    x |= (c0D / y) | (c14 / y);
    x |= (c0C / y) | (c15 / y);
    x |= (c0B / y) | (c16 / y);
    x |= (c0A / y) | (c17 / y);
    x |= (c09 / y) | (c18 / y);
    x |= (c08 / y) | (c19 / y);
    x |= (c07 / y) | (c1A / y);
    x |= (c06 / y) | (c1B / y);
    x |= (c05 / y) | (c1C / y);
    x |= (c04 / y) | (c1D / y);
    x |= (c03 / y) | (c1E / y);
    x |= (c02 / y) | (c1F / y);
    x |= (c01 / y) | (c20 / y);

  break; } break; } break; } break; } break; } break; } break; } break; }
  break; } break; } break; } break; } break; } break; } break; } break; }
  break; } break; } break; } break; } break; } break; } break; } break; }
  break; } break; } break; } break; } break; } break; } break; } break; }

  return x == 0x3F ? 0 : 1;
}
```

# Executing the compiled binary

## ExitCode

```
0
```

# Meta

## Limited to Category

```
false
```

## Creator

```
impeccable-astrapia
```

## Admin Authored

```
false
```

## Hash

```
4ca9ed2fbbd9f46bdee14c3234db0cdcd81182def8890900b7cb68bfc2bdfe43
```
